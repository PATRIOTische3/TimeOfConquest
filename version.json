{
  "_comment": "Version format: MAJOR.MINOR.PATCH-YYMMDD",
  "_rules": [
    "MAJOR — engine rewrites, map changes, save format breaks",
    "MINOR — new gameplay systems (economy, multiplayer, battle queue...)",
    "PATCH — bugfixes, UI tweaks, balance changes",
    "YYMMDD — build date"
  ],
  "version": "0.9.2-260331",
  "changelog": "Fix: loadMapScript now uses <script> tag injection (not new Function) — globals properly exposed; inline map.js always preloaded as default; buttons scope fixed"
}
